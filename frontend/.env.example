# Leave empty in local dev to use Vite proxy to the backend.
# For production builds, set the public API origin.
VITE_API_BASE_URL=

# Optional override for the Vite proxy target (dev only)
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
