import { useEffect, useMemo, useState, type FormEvent } from "react";
import { Link, useSearchParams } from "react-router-dom";
import { api, ApiError } from "../api/client";
import { getAccessToken } from "../auth/storage";
import { useAuth } from "../auth/AuthContext";
type Medication={id:string;code:string;name:string;strength:string|null;form:string|null;status:string};type InventoryItem={id:string;facility_id:string;medication_id:string;current_quantity:number;minimum_quantity:number;status:string};type Prescription={id:string;prescription_id:string;encounter_id:string;patient_id:string;status:string;created_at:string};type BillingService={id:string;code:string;name:string;service_type:string;price:number;status:string};
export function PharmacyPage(){const auth=useAuth();const[params]=useSearchParams();const patientId=params.get("patientId")||"";const encounterId=params.get("encounterId")||"";const[meds,setMeds]=useState<Medication[]>([]);const[inventory,setInventory]=useState<InventoryItem[]>([]);const[prescriptions,setPrescriptions]=useState<Prescription[]>([]);const[services,setServices]=useState<BillingService[]>([]);const[patientName,setPatientName]=useState("");const[patientAfyaId,setPatientAfyaId]=useState("");const[record,setRecord]=useState<any>(null);const[selectedRxId,setSelectedRxId]=useState("");const[serviceByItem,setServiceByItem]=useState<Record<string,string>>({});const[error,setError]=useState<string|null>(null);const[message,setMessage]=useState("");const[loading,setLoading]=useState(true);const[busy,setBusy]=useState(false);const[receipt,setReceipt]=useState({medication_id:"",batch_number:"",expiry_date:"",quantity:"",purchase_price:"",selling_price:""});const selectedRx=useMemo(()=>record?.prescriptions?.find((p:any)=>p.id===selectedRxId),[record,selectedRxId]);const payable=useMemo(()=>record?.billing?.payments?.filter((p:any)=>p.status==="CONFIRMED").reduce((s:number,p:any)=>s+Number(p.amount||0),0)??0,[record]);const charges=useMemo(()=>record?.billing?.charges?.filter((c:any)=>c.encounter_id===(encounterId||selectedRx?.encounter_id)).reduce((s:number,c:any)=>s+Number(c.total_amount||0),0)??0,[record,encounterId,selectedRx]);async function load(){setLoading(true);setError(null);try{const[m,i,r,s]=await Promise.all([api.listMedications(),api.listPharmacyInventory(),api.listPrescriptions(),api.listBillingServices()]);setMeds(m);setInventory(i);setPrescriptions(r);setServices(s);if(patientId){const rec=await api.getPatientRecord(patientId);setRecord(rec);const p=rec.patient;setPatientName([p.first_name,p.middle_name,p.last_name].filter(Boolean).join(" "));setPatientAfyaId(p.afya_id);const match=rec.prescriptions.find((x:any)=>(!encounterId||x.encounter_id===encounterId)&&x.status!=="DISPENSED");if(match)setSelectedRxId(match.id)}}catch(e){setError(e instanceof ApiError?e.code:"PHARMACY_LOAD_FAILED")}finally{setLoading(false)}}useEffect(()=>{void load()},[patientId,encounterId]);async function onReceive(e:FormEvent){e.preventDefault();if(!auth.facilityId)return;setBusy(true);try{await api.receiveInventory({facility_id:auth.facilityId,medication_id:receipt.medication_id,batch_number:receipt.batch_number.trim(),expiry_date:receipt.expiry_date,quantity:Number(receipt.quantity),purchase_price:Number(receipt.purchase_price),selling_price:Number(receipt.selling_price)});setReceipt({medication_id:"",batch_number:"",expiry_date:"",quantity:"",purchase_price:"",selling_price:""});setMessage("Stock receipt posted.");await load()}catch(e){setError(e instanceof ApiError?e.code:"INVENTORY_RECEIPT_FAILED")}finally{setBusy(false)}}async function dispense(){if(!selectedRx)return;const items=selectedRx.items.map((i:any)=>({prescription_item_id:i.id,service_id:serviceByItem[i.id]})).filter((x:any)=>x.service_id);if(items.length!==selectedRx.items.length){setError("SELECT_BILLING_SERVICE_FOR_EACH_MEDICINE");return}if(charges<=0){setError("BILLING_REQUIRED_BEFORE_DISPENSING");return}if(payable<charges){setError("PAYMENT_REQUIRED_BEFORE_DISPENSING");return}setBusy(true);setError(null);try{const base=(import.meta.env.VITE_API_BASE_URL??"").replace(/\/$/,"");const token=getAccessToken();const res=await fetch(`${base}/api/v1/pharmacy/prescriptions/${selectedRx.id}/dispense`,{method:"POST",headers:{"Content-Type":"application/json",...(token?{Authorization:`Bearer ${token}`}:{})},body:JSON.stringify({billing_items:items})});if(!res.ok){let b:any=null;try{b=await res.json()}catch{}throw new Error(b?.detail||"DISPENSE_FAILED")}const result=await res.json();setMessage(`Dispensed successfully. ${result.movements_created} stock movement(s) recorded.`);await load()}catch(e){setError(e instanceof ApiError?e.code:e instanceof Error?e.message:"DISPENSE_FAILED")}finally{setBusy(false)}}return <section className="page-stack pharmacy-page"><header className="page-heading"><div><p className="eyebrow">Medication workflow</p><h1>Pharmacy</h1><p className="muted">Receive the named patient, verify billing and confirmed payment, dispense the prescription and finish the medication stage.</p></div><button className="button secondary" onClick={()=>void load()}>Refresh</button></header>{patientId&&<section className="card patient-context"><div><p className="eyebrow">Patient at pharmacy</p><h2>{patientName}</h2><p className="muted">{patientAfyaId} · {encounterId?`Encounter ${encounterId}`:""}</p></div><div className="actions"><Link className="button secondary" to={`/patients/${patientId}/journey`}>Open care journey</Link><span className="status-pill">RECEIVED</span></div><div className="pharmacy-payment"><span>Charges</span><strong>KES {charges.toLocaleString()}</strong><span>Confirmed payment</span><strong>KES {payable.toLocaleString()}</strong></div></section>}{error&&<div className="error">{error}</div>}{message&&<div className="success-box">{message}</div>}{loading?<p>Loading pharmacy…</p>:<>{patientId&&<article className="card"><div className="row-between"><div><p className="eyebrow">Patient prescription</p><h2>{patientName}</h2><p className="muted">Prescription and stock actions are tied to this encounter.</p></div>{selectedRx&&<span className="status-pill">{selectedRx.status}</span>}</div>{!selectedRx?<div className="notice"><strong>No undispensed prescription.</strong><p>Return to clinical care and create the prescription first.</p><Link className="button secondary" to={`/encounters/${encounterId}`}>Return to encounter</Link></div>:<><div className="table-wrap"><table><thead><tr><th>Medicine</th><th>Directions</th><th>Qty</th><th>Billing service</th></tr></thead><tbody>{selectedRx.items.map((i:any)=><tr key={i.id}><td><strong>{i.name} {i.strength||""}</strong></td><td>{i.dose} · {i.frequency} · {i.duration}</td><td>{i.quantity}</td><td><select value={serviceByItem[i.id]||""} onChange={e=>setServiceByItem(c=>({...c,[i.id]:e.target.value}))}><option value="">Select service</option>{services.filter(s=>s.status==="ACTIVE").map(s=><option key={s.id} value={s.id}>{s.name} · KES {Number(s.price).toLocaleString()}</option>)}</select></td></tr>)}</tbody></table></div><div className="notice"><strong>Billing and payment gate</strong><p>The patient must have an encounter charge and sufficient confirmed payment before stock is dispensed.</p><button className="button" disabled={busy||charges<=0||payable<charges||selectedRx.status==="DISPENSED"} onClick={()=>void dispense()}>{busy?"Processing…":charges<=0?"Billing required":payable<charges?"Payment required":"Dispense medicines"}</button></div></>}</article>}<article className="card"><h2>Facility inventory ({inventory.length})</h2><div className="table-wrap"><table><thead><tr><th>Medication</th><th>Current quantity</th><th>Minimum</th><th>Status</th></tr></thead><tbody>{inventory.map(i=><tr key={i.id}><td>{meds.find(m=>m.id===i.medication_id)?.name||i.medication_id}</td><td>{i.current_quantity}</td><td>{i.minimum_quantity}</td><td>{i.status}</td></tr>)}</tbody></table></div></article><article className="card"><h2>Receive medicines into stock</h2><form className="form-grid" onSubmit={onReceive}><label className="full">Medicine<select required value={receipt.medication_id} onChange={e=>setReceipt({...receipt,medication_id:e.target.value})}><option value="">Select medicine</option>{meds.map(m=><option key={m.id} value={m.id}>{m.name} · {m.code}</option>)}</select></label><label>Batch<input required value={receipt.batch_number} onChange={e=>setReceipt({...receipt,batch_number:e.target.value})}/></label><label>Expiry<input required type="date" value={receipt.expiry_date} onChange={e=>setReceipt({...receipt,expiry_date:e.target.value})}/></label><label>Quantity<input required type="number" min="0.01" step="0.01" value={receipt.quantity} onChange={e=>setReceipt({...receipt,quantity:e.target.value})}/></label><label>Purchase/unit<input required type="number" min="0" step="0.01" value={receipt.purchase_price} onChange={e=>setReceipt({...receipt,purchase_price:e.target.value})}/></label><label>Selling/unit<input required type="number" min="0" step="0.01" value={receipt.selling_price} onChange={e=>setReceipt({...receipt,selling_price:e.target.value})}/></label><div className="full actions"><button disabled={busy}>Post stock receipt</button></div></form></article></>}</section>}
